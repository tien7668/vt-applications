base-service:
  nameOverride: simple-go-1-main
  fullnameOverride: &name simple-go-1-main
  server:
    revisionHistoryLimit: 1
    image:
      repository: asia.gcr.io/fast-audio-352608/core/simple-go-1
      tag: main

    serviceAccount:
      create: true

    overrideCommand:
      - "/app/server"
      - "main"

    resources:
      limits:
        cpu: 1000m
        memory: 2Gi
      requests:
        cpu: 200m
        memory: 200Mi
    expose:
      port: "8080"
